// ---------------------------------------------------------------------------
// Extension key
// ---------------------------------------------------------------------------

export const ATTESTATION_KEY = 'attestation'

/**
 * Default attestation key. Pass a custom key to `createAttestationExtension`
 * to support multiple attestors on the same server.
 */
export const DEFAULT_ATTESTATION_KEY = ATTESTATION_KEY
